@model Northwind.Models.CatProductListViewModel


@{
    ViewData["Title"] = "Index";
}

    <div class="">
        <h2>@Model.catName</h2>
        <!--
        <div class="list-group">
            @foreach (Northwind.Models.Product p in Model.productList)
            {
                <a href="#" class="list-group-item list-group-item-action list_btn">@p.ProductName | $@p.UnitPrice</a>
            }
        </div>
        -->
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">ID#</th>
                    <th scope="col">Product Name</th>
                    <th scope="col">Unit Price</th>
                    <th scope="col">product Info</th>
                </tr>
            </thead>
            <tbody>
                @foreach (Northwind.Models.Product p in Model.productList)
                {
                
                        <tr>
                            <th scope="row">@p.ProductId</th>
                            <td>@p.ProductName</td>
                            <td>$@p.UnitPrice</td>
                            <td><a asp-controller="ProductDetail" asp-action="Index" asp-route-id="@p.ProductId">More Info</a> </td>
                        </tr>
                    
                        }

                </tbody>
        </table>

    </div>

